// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "dark-mode-daemon",
    dependencies: [ ],
    targets: [
        .target(
            name: "dark-mode-daemon",
            path: "."
        )
    ]
)
